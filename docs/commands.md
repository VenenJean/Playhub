Change branch: git checkout master

Get changes: git pull

Change branch: git checkout antons-branch

Merge code: git merge master

Push changes remote: git push

git add filename1.php path/to/filename2.php
git commit -m "Nachricht"
git push

> weg zum Master dann daten holen, geht zurück und holt vom master alle daten und gibt zu github

git checkout master; git pull; git checkout antons-branch; git merge master; git push;
